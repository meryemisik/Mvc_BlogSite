@model MI.AriBilgi.DLL.Categories

@{
    ViewBag.Title = "CategoryDelete";
    Layout = "~/Areas/AdminPaneli/Views/Shared/AdminLayout.cshtml";
}


<div style="margin:8px;">

    <section class="content" style="margin-top:8px;">
        <div class="row">
            <div class="col-md-12">
                <div class="card card-success">
                    <div class="card-header">
                        <h3 class="card-title">KATEGORİ SİL</h3>

                        <div class="card-tools">
                            <button type="button" class="btn btn-tool" data-card-widget="collapse" data-toggle="tooltip" title="Collapse">
                                <i class="fas fa-minus"></i>
                            </button>
                        </div>
                    </div>
                    <div class="card-body" style="display: block;">

                        <div>
                            <dl class="dl-horizontal">
                                <dt>
                                    @Html.DisplayNameFor(model => model.CategoryName)
                                </dt>

                                <dd>
                                    @Html.DisplayFor(model => model.CategoryName)
                                </dd>

                            </dl>

                            @using (Html.BeginForm())
                            {
                                @Html.AntiForgeryToken()

                        <div class="form-actions no-color">
                            <input type="submit" value="Kategori Sil" class="btn btn-success float-right">
                            @Html.ActionLink("Kategori Listesine Git", "CategoriesAdminIndex", "", new { @class = "btn btn-secondary" })

                        </div>
                            }
                        </div>
                        </div>
                    </div>
                </div>
            </div>
        </section>
    </div>
